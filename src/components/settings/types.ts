import type { SongEditFormProps } from "./SongEditForm.tsx"
import type { LyricsEditorProps } from "./LyricsEditor.tsx"

export interface SongEditPanelProps {
    form: SongEditFormProps
    lyricsEditor: LyricsEditorProps
}
